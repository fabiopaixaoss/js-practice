// Criando uma função

// Declara a função e diz o que ela faz
function imprimeTexto (texto) {
    console.log(texto)
}
// Executa a função (1 ou + vezes)
// Só usamos função quando precisamos que um bloco de códigos seja executado no momento em que a gente quer 

imprimeTexto("Olá!")

function soma () {
    return 2 + 2;
}

console.log(soma())